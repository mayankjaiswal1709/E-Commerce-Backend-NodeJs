// const joi = require('joi')

// const productValidationSchema = {
//     addProducts: joi
//         .object({
//             product_name: joi.string().min(4).max(20).required(),
//             product_description: joi.string().min(5).max(50).required(),
//             product_price: joi.number().integer().min(10).max(999999).required(),
//         })
//         .unknown(true),

// };
// module.exports = productValidationSchema;